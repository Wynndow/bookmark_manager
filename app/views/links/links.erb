<!DOCTYPE html>
<html>
<head>
  <title> Links </title>
</head>
<body>

  <h1> Links </h1>

  <ul id=links>
    <% @links.each do |link| %>
      <li>
        Title: <%=link.title%>
        URL: <a href="http://<%=link.url%>"><%=link.url%></a>
        Tag: <%= link.tags.map(&:tag).join(', ') %>
      </li>
    <% end %>
  </ul>

</body>
</html>
